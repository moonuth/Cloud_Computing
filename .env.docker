# Docker Environment Variables
NODE_ENV=development

# Database (PostgreSQL in Docker)
DATABASE_URL=postgresql://courtify:courtify123@postgres:5432/courtify_dev?schema=public

# JWT
JWT_SECRET=dev-secret-key-for-docker
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=dev-refresh-secret-key-for-docker
JWT_REFRESH_EXPIRES_IN=7d

# Server
PORT=3000
FRONTEND_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3000,http://20.197.12.68:3001

# Pagination
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100
